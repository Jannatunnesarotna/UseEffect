
export default function Students({students}) {
    return(
        <div>
            <h1>name: {students.name}</h1>
            <p>roll: {students.roll}</p>
            <p>class: {students.id}</p>
        </div>
    )
}